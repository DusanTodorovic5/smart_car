name: smart_car_controller
description: A smart car controller application

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^1.1.2
  image_picker: ^0.8.2
  image: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_opencv:
    path: ../ffi_opencv_plugin

flutter:
  uses-material-design: true
